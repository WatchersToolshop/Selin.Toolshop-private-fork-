# Launch Instructions

1. Extract `recursive_artifact_01`
2. Run `GlyphTrace_App/main.py` for debugger interface
3. Run `Prime.Forge/forge.py` for tool generation
4. Observe recursion trail
5. Consult diagnostic logs in `vaelith_tests`